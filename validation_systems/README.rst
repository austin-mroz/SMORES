Validation
==========

This directory holds the scripts required for the validation of
``SMORES``. There are mutiple validations wihch can be performed
on ``SMORES``, and each is held and documented in a subdirectory.
